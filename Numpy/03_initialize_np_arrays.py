import numpy as np

# create a 2*3 array (matrix), all initialised with zeroes
n_zeros = np.zeros([2,3])
print(n_zeros)
print(type(n_zeros))

# 3*3 matrix all initialised with 1
n_ones = np.ones([3,3])
print(n_ones)